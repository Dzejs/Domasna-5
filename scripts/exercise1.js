let arr = [];
for(let i = 1; i <= 20; i++){     
    if(i % 2 === 0){
       let rez = (`${i}`);
       console.log(rez);
       console.log("\n");
    }
    else{
        let rez = (`${i} " "`);
        console.log(rez);
    }
}
